import { NomousedownDirective } from './nomousedown.directive';

describe('NomousedownDirective', () => {
  it('should create an instance', () => {
    const directive = new NomousedownDirective();
    expect(directive).toBeTruthy();
  });
});
