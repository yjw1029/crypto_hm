import { NokeyboardDirective } from './nokeyboard.directive';

describe('NokeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new NokeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
